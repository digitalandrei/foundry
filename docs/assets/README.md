# Assets

Currently empty. The original dark-mode dashboard mockup
(`dashboard-mockup-dark.png`, shared in conversation 2026-06-11) was never
committed and the shipped UI has superseded it — `docs/UI-DESIGN.md` is the
authoritative design contract.
