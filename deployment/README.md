# Deployment Artifacts

Deployable truth for `docs/DEPLOYMENT.md` — keep both in sync.

Planned contents (drafted in Phase 2, finalized in Phase 10):

- `systemd/foundry-controller.service`
- `nginx/foundry.cloudcraft.ro.conf` (Cloudflare real-IP, long-poll
  timeouts, rate limits)
- `agent/` — foundry-agent install script + `foundry-agent.service`
