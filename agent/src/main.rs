//! Foundry GPU-server agent: pull-based task loop, Docker executors,
//! NVML inventory. Skeleton only — the task loop lands in Phase 2.

fn main() {
    println!("foundry-agent {}", env!("CARGO_PKG_VERSION"));
}
