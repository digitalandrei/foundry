# Frontend

React + TypeScript + Vite + shadcn/ui SPA. The scaffold (Vite init, theme
tokens, layout shell) lands in Phase 2 — see `docs/plans/phase-02.md`.
Design contract: `docs/UI-DESIGN.md`; code rules: `docs/FRONTEND_RULES.md`.
