//! Foundry control plane: axum API, GitLab integration, scheduler state,
//! agent task queue. Skeleton only — the axum app lands in Phase 2.

fn main() {
    println!("foundry-controller {}", env!("CARGO_PKG_VERSION"));
}
